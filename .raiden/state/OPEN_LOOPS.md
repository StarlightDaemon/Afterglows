# Open Loops

No open loops at this time. Site is in active maintenance; no blocking work identified.

---

## Closed

- **2026-06-07** — WSL→macOS migration remediation complete. All `/mnt/e/` paths corrected to `/Users/dante/Citadel/` across AGENTS.md, prompt files, and state files. `commit-msg` hook execute bit set. `node_modules` reinstalled clean on ARM64 macOS. Edict v0.6.1 confirmed clean.
